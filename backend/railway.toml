# Sobrescribir el archivo con la versión correcta
(
echo [build]
echo builder = "dockerfile"
echo dockerfilePath = "Dockerfile"
echo.
echo [deploy]
echo healthcheckPath = "/"
echo healthcheckTimeout = 30
echo restartPolicyType = "on_failure"
) > backend\railway.toml
