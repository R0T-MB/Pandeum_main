/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com', 'pandeum.com'], // para avatares de Google
  },
  reactStrictMode: true,
}

module.exports = nextConfig